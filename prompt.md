You are continuing development of JARVIS, Janon Emersion T’s private local AI workstation assistant.

IMPORTANT:
Before generating ANY code, you MUST first inspect the actual repository structure and current implementation state. Do not assume architecture, filenames, imports, helper functions, or existing safety logic.

You must first:

1. Read the repo structure.
2. Inspect existing route modules.
3. Inspect current patch/safety systems.
4. Inspect current project context logic.
5. Inspect existing DevOps and health tools.
6. Detect reusable helper functions.
7. Detect existing memory/task infrastructure.
8. Detect current safety limitations.
9. Detect current write/execution flows.
10. Only then design the next implementation.

Never invent architecture that conflicts with the repo.

Current architecture status:

* Router is modularized.
* `core/command_router.py` must remain lightweight.
* Features must be added using dedicated modules.
* Route handlers are separated into route files.
* Patch proposal workflow already exists.
* SafeWriter and proposal manager already exist.
* Project context system already exists.
* Project health tools already exist.
* AI fallback is already separated.

Tech stack:

* Python
* FastAPI
* Playwright
* Offline-first local architecture
* Git-based workflow
* Modular route-driven system

Critical architectural rules:

* Never bloat `command_router.py`
* Prefer reusable helper utilities
* Prefer safe/read-only workflows first
* Dangerous actions must require confirmation
* Production structure only
* No tutorial-style architecture
* Preserve backward compatibility
* Respect existing modular design

Current repository:

* GitHub repo: https://github.com/Janon-Emersion-T/jet.git
* Local project already operational

this is one of the most important part and teach me things clearly on how to implement this

when we finish it successfully we can push it to git

no need to add unwanted comments

name this chat as 351

Next phases to implement:
351

351. Ticket prioritization engine
